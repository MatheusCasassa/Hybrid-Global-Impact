import React from 'react'

import {
    SafeAreaView,
    Text
} from 'react-native'

import {
    Button
} from 'react-native-elements'

import { clear } from '../../DB';

const Home = () => {

    return (
        <SafeAreaView>
            <Text>Salve</Text>
        </SafeAreaView>
    )
}

export default Home